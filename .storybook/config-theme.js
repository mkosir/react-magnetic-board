import { create } from '@storybook/theming/create';

export default create({
  base: 'light',

  brandTitle: 'React Magnetic Board 🧲',
  brandUrl: 'https://github.com/mkosir/react-magnetic-board',
});
